// Use the slice() method to create a new array with a subset(first half of array) of elements from an existing array

const arr = ["dog", "bird", "elephant", "cat", "unicorn", "dragon", "hippogriff"];

const half = arr.slice(0, 4);

console.log(half);
